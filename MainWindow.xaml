<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PhoneAudioLink.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhoneAudioLink"
    xmlns:enumeration="using:Windows.Devices.Enumeration"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tb="using:H.NotifyIcon"
    mc:Ignorable="d"
    Title="Phone Audio Link">

    <Grid x:Name="MainGrid" Loaded="MainGrid_Loaded" Padding="24" RowSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Audio Devices" Style="{ThemeResource TitleTextBlockStyle}" VerticalAlignment="Center"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <SymbolIcon Symbol="Link" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <TextBlock x:Name="ConnectionState" Text="Disconnected" 
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" 
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" 
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                BorderThickness="1" 
                CornerRadius="8" 
                Padding="4">

            <ListView x:Name="DeviceListView" 
                      ItemsSource="{x:Bind devices}" 
                      SelectionChanged="DeviceListView_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="enumeration:DeviceInformation">
                        <Grid Padding="8,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <SymbolIcon Symbol="CellPhone" Margin="0,0,16,0" VerticalAlignment="Center" 
                                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"/>

                            <TextBlock Grid.Column="1" Text="{x:Bind Name}" 
                                       VerticalAlignment="Center" 
                                       Style="{ThemeResource BodyTextBlockStyle}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <Grid Grid.Row="2">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="12">
                <ProgressRing x:Name="LoadingSpinner" IsActive="False" Visibility="Collapsed" 
                              Width="20" Height="20" VerticalAlignment="Center"/>

                <Button x:Name="EnableAudioPlaybackConnectionButton" 
                        Content="Connect" 
                        Click="EnableAudioPlaybackConnectionButton_Click"
                        Style="{ThemeResource AccentButtonStyle}"
                        IsEnabled="False"
                        Width="120"/>
            </StackPanel>
        </Grid>

        <tb:TaskbarIcon x:Name="TrayIcon" IconSource="/Assets/PhoneAudioLink.ico" ToolTipText="PhoneAudioLink" PopupActivation="DoubleClick" Visibility="Collapsed">
            <tb:TaskbarIcon.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutItem Text="Open" Click="OpenMenuFlyoutItem_Click"/>
                    <MenuFlyoutItem Text="Exit" Click="ExitMenuFlyoutItem_Click"/>
                </MenuFlyout>
            </tb:TaskbarIcon.ContextFlyout>
        </tb:TaskbarIcon>
    </Grid>
</Window>